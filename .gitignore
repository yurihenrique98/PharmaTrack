# Dependencies
/node_modules
/.pnp
.pnp.*

# Next.js 
/.next/
/out/
/build

# --- SECRETS  ---
.env
.env.local
.env.development.local
.env.production.local
# Ignore all JSON files by default (to catch Firebase keys)
*.json
# EXCEPT these three which are MANDATORY for the app to work:
!package.json
!package-lock.json
!tsconfig.json

# --- SYSTEM & DEBUG ---
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- TYPESCRIPT ---
*.tsbuildinfo
next-env.d.ts